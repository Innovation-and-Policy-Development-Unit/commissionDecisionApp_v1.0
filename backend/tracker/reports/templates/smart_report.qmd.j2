---
title: "{{ title | e }}"
{% if subtitle %}subtitle: "{{ subtitle | e }}"
{% endif %}author: "SCDMS — Public Service Commission of Vanuatu"
date: "{{ generated_at | e }}"
format:
  html:
    theme: cosmo
    toc: true
    toc-depth: 2
    embed-resources: true
    page-layout: full
jupyter: python3
execute:
  echo: false
  warning: false
lang: {{ lang }}
---

```{python}
#| echo: false
import json
from pathlib import Path
import render_helpers as rh

data = json.loads(Path("data.json").read_text(encoding="utf-8"))
spec = json.loads(Path("spec.json").read_text(encoding="utf-8"))
agg = data.get("aggregates", {})
rows = data.get("rows", [])
meta = data.get("meta", {})
try:
    HIGHCHARTS_JS = Path("highcharts.bundle.js").read_text(encoding="utf-8")
except Exception:
    HIGHCHARTS_JS = ""
```

```{python}
#| output: asis
print(rh.REPORT_CSS)
```

**Prepared for:** {{ generated_by | e }} · **Records in scope:** {{ row_count }}
{% if user_prompt %}

> {{ user_prompt | e }}
{% endif %}
{% if narrative %}

## Executive summary

{{ narrative }}
{% endif %}

## Overview

```{python}
#| output: asis
print(rh.render_kpis(spec, agg))
```

```{python}
#| output: asis
print(rh.render_highcharts(spec, agg, HIGHCHARTS_JS))
```

## Detail

```{python}
#| output: asis
print(rh.render_table(rows, spec.get("table", {}).get("columns", [])))
```

```{python}
#| output: asis
if meta.get("truncated"):
    print(f"<p><em>Showing {meta.get('rows_shown')} of {meta.get('row_count')} "
          "records. Refine the date range or filters to narrow the report.</em></p>")
```

---

*Generated by SCDMS Enterprise Reporting — {{ generated_at | e }}*
