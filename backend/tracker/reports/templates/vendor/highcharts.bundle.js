/* PLACEHOLDER — Highcharts is NOT bundled in source control (proprietary license).

   To enable interactive charts in Smart Reports, replace this file with a real,
   self-contained Highcharts build, e.g. the concatenation of:
     - highcharts.js
     - modules/exporting.js        (optional)
     - modules/accessibility.js    (recommended)
   from https://www.highcharts.com/download/ (a valid license is required for
   production use — confirmed by the product owner for this deployment).

   Until a real bundle is present, Smart Reports render every chart as a static
   HTML table fallback (see reports/render_helpers.py: highcharts_available()), so
   reports still generate correctly — just without interactive visualizations.

   The file is read verbatim and inlined into the report HTML (embed-resources),
   so the generated reports are fully self-contained and work offline. */
